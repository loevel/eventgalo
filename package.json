{
  "name": "eventgalo",
  "private": true,
  "packageManager": "pnpm@10.30.2",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "deploy": "turbo deploy",
    "db:migrate:local": "pnpm --filter @eventgalo/api db:migrate:local",
    "db:migrate:remote": "pnpm --filter @eventgalo/api db:migrate:remote"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
