{
  "name": "@eventgalo/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply eventgalo-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply eventgalo-db --remote"
  },
  "dependencies": {
    "@eventgalo/shared": "workspace:*",
    "hono": "^4.12.30",
    "stripe": "^22.3.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260716.1",
    "typescript": "^5.8.0",
    "wrangler": "^4.111.0"
  }
}
